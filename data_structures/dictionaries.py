#literally just objects from javascript
my_dict = {
  'age': 25, 
  'name': 'Gary',
}

print(my_dict['age'])
print(my_dict.keys()) # returns an array containing keys
print(my_dict.values()) # returns an array of values
print(my_dict.items()) # return key value pairs as tuples